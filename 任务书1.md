# 工作计划：bot.js 增加图片与 PDF 文件理解能力

> **目标**：让微信 AI Bot 支持上传图片和 PDF，基于文件内容进行多轮对话。
> **范围**：扩展 `bot.js` 文件处理链路，新增本地 OCR 微服务（PDF 处理）。
> **预计工作量**：阶段一约 2-3 小时，阶段二约 4-6 小时。

---

## 项目背景

### 现状
- `bot.js` 已支持 `.docx`（mammoth 抽文本）、`.txt` 文件
- 文件先暂存到 `pendingFiles` Map（30 分钟 TTL），等用户下一条消息合并传给 AI
- AI 接口走 B.AI 中转站的 `/v1/messages`（Claude Messages 格式）
- 部署在飞牛 NAS，PM2 管理，Node.js v22，工作目录 `/opt/weixin-bot-ai/instances/<name>/`

### 中转站能力边界（已确认）
- ✅ 支持 `image` content block（JPEG/PNG/GIF/WebP，base64 或 URL）
- ❌ **不支持** `document` block（Anthropic 官方的 PDF 原生通道未暴露）
- ❌ **不支持** Files API（无法预上传复用）

### 设计决策

**PDF 处理走分流策略**：

| 页数 | 处理方式 | 理由 |
|------|---------|------|
| ≤ 3 页 | 转图片走 Claude vision | 保留版式/图表，token 成本可控 |
| > 3 页 | 本地 RapidOCR 抽文本 | 省 token，扫描件也能处理 |

**图片**：直接走 Claude vision，无本地 OCR 兜底。

---

## 阶段一：图片支持 + PDF 转图片走 vision

### 目标
- 用户发图片 → Bot 用 Claude vision 理解 → 回复
- 用户发 ≤3 页 PDF → Bot 转图片走 vision → 回复
- 用户发 >3 页 PDF → 暂时降级提示"暂不支持，正在开发中"（阶段二实现）

### Task 1.1：改造 `callAI()` 支持 content blocks 数组

**位置**：`bot.js` 内 `callAI()` 函数

**改动点**：
- 当前 `messages: [...history, { role: "user", content: userMessage }]` 中 `content` 是字符串
- 改为支持字符串或 content blocks 数组：
  ```js
  // 字符串（向后兼容，纯文本场景）
  { role: "user", content: "你好" }
  // 数组（多模态场景）
  { role: "user", content: [
      { type: "image", source: { type: "base64", media_type: "image/jpeg", data: "..." }},
      { type: "text", text: "这张图说了什么？" }
  ]}
  ```

**函数签名变更**：
```js
// 旧
async function callAI(userMessage, config, history = [])
// 新
async function callAI(userContent, config, history = [])
// userContent 可以是 string 或 ContentBlock[]
```

**关键实现点**：
- 历史消息 `addToHistory` 时，如果新消息是数组，只保留其中 text 部分入历史（避免历史里塞 base64 图片爆 token）
- 文本截断逻辑 `HISTORY_MAX_CONTENT_LEN` 仍然只对 text 生效

**验收**：调用 `callAI([{type:"text", text:"你好"}], config)` 和 `callAI("你好", config)` 行为一致。

---

### Task 1.2：消息循环识别图片消息（`type=2`）

**位置**：`bot.js` 内 `messageLoop()` 中 `firstItem?.type !== 1` 那段

**改动**：
- 新增分支：`firstItem?.type === 2`（图片消息）
- 图片消息结构需要看实际的 iLink API 返回字段（参考已有的 `firstItem.type === 4` 文件分支结构推断）
- 关键字段预期：`image_item.media.full_url` + `image_item.media.aes_key` + `image_item.media.encrypt_query_param`

**实现策略**：
1. 复用现有 `downloadAndDecryptFile()` 逻辑（AES-128-ECB 解密），抽取成更通用的 `downloadAndDecryptMedia()`
2. 检测图片格式（JPEG/PNG/WebP）— 用文件头 magic number 判断更稳，不依赖文件名
3. 转 base64
4. 暂存到 `pendingFiles`，结构扩展为 `{ type: "image" | "document_text" | "document_image", ... }`

**新的 pendingFiles 数据结构**：
```js
// 文本类（docx/txt/OCR结果）
{ type: "text", fileName, text, timestamp }
// 图片类（图片消息、PDF小于等于3页转图）
{ type: "image", fileName, images: [{mediaType, base64}], timestamp }
```

**验收**：用户发一张图，bot 回复"已收到图片 xxx，请告诉我您的要求"，30 分钟内的下一句话触发图文一起送 AI。

---

### Task 1.3：抽象 `sendAiReply()` 适配两种 pending 类型

**位置**：`bot.js` 内 `sendAiReply()` 和触发它的地方

**改动**：
当 `pendingFile` 存在时，构造 content blocks：

```js
if (pendingFile.type === "text") {
  // 现有逻辑
  const combined = `[用户之前发送了文件: ${pendingFile.fileName}]\n\n${pendingFile.text}\n\n[用户要求]\n${text}`;
  await sendAiReply(fromId, contextToken, combined);
} else if (pendingFile.type === "image") {
  const content = [
    ...pendingFile.images.map(img => ({
      type: "image",
      source: { type: "base64", media_type: img.mediaType, data: img.base64 }
    })),
    { type: "text", text: `[用户上传了: ${pendingFile.fileName}]\n${text}` }
  ];
  await sendAiReply(fromId, contextToken, content);
}
```

`sendAiReply()` 内部把 `userPrompt` 参数改为 `userContent`，传给 `callAI()`。

**验收**：图片+"这张图说了什么" → AI 正确描述图片内容。

---

### Task 1.4：PDF 处理 - 页数判断 + 转图

**位置**：`bot.js` 内文件消息处理分支（`firstItem?.type === 4`），新增 `.pdf` 扩展名分支

**实现**：

1. **安装依赖**：
   ```bash
   npm install pdfjs-dist canvas
   ```
   - `pdfjs-dist`：Mozilla 的 PDF.js，纯 JS 解析 PDF，无原生依赖
   - `canvas`：node-canvas，用于渲染页面到位图。**注意**：node-canvas 在飞牛 NAS 上需要装系统依赖 `libcairo2-dev libpango1.0-dev libjpeg-dev libgif-dev librsvg2-dev`，如果编译失败，改用 `@napi-rs/canvas`（预编译二进制，无需系统库）

2. **新增函数 `extractFromPdf(buffer)`**：
   ```js
   async function extractFromPdf(buffer) {
     const pdfjsLib = await import("pdfjs-dist/legacy/build/pdf.mjs");
     const doc = await pdfjsLib.getDocument({ data: buffer }).promise;
     const pageCount = doc.numPages;

     if (pageCount <= 3) {
       // 转图片
       const images = [];
       for (let i = 1; i <= pageCount; i++) {
         const page = await doc.getPage(i);
         const viewport = page.getViewport({ scale: 2.0 }); // 2x 提升 OCR 质量
         // 渲染到 canvas，输出 PNG base64
         // ...具体实现见 pdfjs-dist 文档
         images.push({ mediaType: "image/png", base64: pngBase64 });
       }
       return { mode: "image", pageCount, images };
     } else {
       // 阶段二实现，先返回标识
       return { mode: "ocr_needed", pageCount };
     }
   }
   ```

3. **集成到文件分支**：
   ```js
   if (ext === "pdf") {
     const result = await extractFromPdf(fileData.buffer);
     if (result.mode === "image") {
       pendingFiles.set(fromId, {
         type: "image",
         fileName: fileItem.file_name,
         images: result.images,
         timestamp: Date.now()
       });
       await sendMsgSafe(fromId, contextToken,
         `已收到「${fileItem.file_name}」（${result.pageCount}页），请告诉我您的要求。`);
     } else {
       await sendMsgSafe(fromId, contextToken,
         `已收到「${fileItem.file_name}」（${result.pageCount}页），超过3页的PDF识别功能正在开发中，敬请期待。`);
     }
   }
   ```

**验收**：
- 1-3 页 PDF：触发图片流程，AI 能基于 PDF 内容回答
- 4+ 页 PDF：友好提示

---

### Task 1.5：xlsx 和 pptx 顺手加上（可选，建议同期完成）

**理由**：技术栈成熟，工作量小，趁手做一起回归测试。

**依赖**：
```bash
npm install xlsx                    # SheetJS，处理 xlsx
npm install node-pptx-parser        # 处理 pptx
```

**实现**：扩展 `extractTextFromFile()` switch case：
```js
if (ext === "xlsx" || ext === "xls") {
  const XLSX = await import("xlsx");
  const wb = XLSX.read(buffer);
  // 每个 sheet 转 CSV 或 Markdown 表格，拼接
  const text = wb.SheetNames.map(name => {
    const csv = XLSX.utils.sheet_to_csv(wb.Sheets[name]);
    return `## Sheet: ${name}\n\n${csv}`;
  }).join("\n\n");
  return { text, type: "xlsx" };
}
if (ext === "pptx") {
  const { default: PPTXParser } = await import("node-pptx-parser");
  // 抽取每页文字
  // 注意：node-pptx-parser 只能拿文字，PPT 里的图片会丢失
  // 如果用户场景以图为主，需要走"PPT 转 PDF 转图"路径，阶段二再考虑
}
```

**验收**：xlsx 抽出来的 CSV 能被 AI 正确理解（"这表第3行说了什么"）。

---

### 阶段一交付物
- [ ] `bot.js` 改造完成，向后兼容（纯文本对话不受影响）
- [ ] 图片消息端到端跑通
- [ ] ≤3 页 PDF 端到端跑通
- [ ] xlsx/pptx 文本抽取跑通（可选）
- [ ] PM2 重启后旧实例正常工作
- [ ] 在测试实例上至少跑过：图片 OCR、图片描述、PDF 截图问答、Excel 数据问答各一遍

---

## 阶段二：本地 RapidOCR 微服务，处理 >3 页 PDF

### 目标
部署 Python RapidOCR 微服务，bot.js 通过 HTTP 调用，把超过 3 页的 PDF 转图后逐页 OCR，拼接文本后走 text 通道喂 Claude。

### 架构

```
┌─────────────┐         HTTP          ┌──────────────────────┐
│   bot.js    │ ─────────────────────>│ ocr-service (Python) │
│  (Node)     │                       │  FastAPI + RapidOCR  │
│  PM2 进程   │ <─────────────────────│  PM2 进程            │
└─────────────┘    {pages: [text]}    └──────────────────────┘
```

OCR 服务和 bot 解耦，独立 PM2 管理，挂了不影响 bot 主流程。

---

### Task 2.1：搭建 OCR 微服务

**目录结构**：
```
/opt/weixin-bot-ai/
└── ocr-service/
    ├── main.py              # FastAPI 入口
    ├── requirements.txt
    ├── venv/                # Python 虚拟环境
    └── logs/
```

**`requirements.txt`**：
```
fastapi==0.115.0
uvicorn==0.32.0
rapidocr-onnxruntime==1.3.24
python-multipart==0.0.12
pillow==11.0.0
```

**`main.py` 核心实现**（开发者可基于以下骨架展开）：
```python
from fastapi import FastAPI, File, UploadFile, HTTPException
from rapidocr_onnxruntime import RapidOCR
from PIL import Image
import io, base64

app = FastAPI()
ocr = RapidOCR()  # 进程启动时加载模型，常驻内存

@app.post("/ocr/image")
async def ocr_image(file: UploadFile = File(...)):
    """对单张图片做 OCR，返回文本"""
    buf = await file.read()
    img = Image.open(io.BytesIO(buf))
    result, _ = ocr(img)
    if not result:
        return {"text": "", "lines": []}
    lines = [item[1] for item in result]  # item = [box, text, confidence]
    return {"text": "\n".join(lines), "lines": lines}

@app.post("/ocr/batch")
async def ocr_batch(files: list[UploadFile] = File(...)):
    """批量 OCR，给 PDF 多页用，返回每页文本"""
    pages = []
    for i, file in enumerate(files):
        buf = await file.read()
        img = Image.open(io.BytesIO(buf))
        result, _ = ocr(img)
        text = "\n".join([item[1] for item in result]) if result else ""
        pages.append({"page": i + 1, "text": text})
    return {"pages": pages}

@app.get("/health")
async def health():
    return {"status": "ok", "model": "rapidocr-onnxruntime"}
```

**启动命令**：
```bash
cd /opt/weixin-bot-ai/ocr-service
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt
uvicorn main:app --host 127.0.0.1 --port 8765
```

**验收**：
- `curl -F "file=@test.png" http://127.0.0.1:8765/ocr/image` 返回识别文本
- 首张图冷启动 ≤ 5 秒，第二张起每张 1-2 秒

---

### Task 2.2：用 PM2 托管 OCR 服务

**在 `ecosystem.config.cjs` 添加**：
```js
module.exports = {
  apps: [
    // ... 现有的 bot 实例配置
    {
      name: "ocr-service",
      script: "venv/bin/uvicorn",
      args: "main:app --host 127.0.0.1 --port 8765",
      cwd: "/opt/weixin-bot-ai/ocr-service",
      interpreter: "none",  // 直接执行 uvicorn 二进制
      autorestart: true,
      max_restarts: 10,
      out_file: "/opt/weixin-bot-ai/ocr-service/logs/out.log",
      error_file: "/opt/weixin-bot-ai/ocr-service/logs/err.log",
    }
  ]
};
```

**操作**：
```bash
pm2 start ecosystem.config.cjs --only ocr-service
pm2 save
```

**验收**：`pm2 status` 显示 ocr-service `online`，重启 NAS 后自动拉起。

---

### Task 2.3：bot.js 接入 OCR 微服务

**配置**：在 `config.json` 添加：
```json
{
  "ocr_service_url": "http://127.0.0.1:8765"
}
```

**新增函数**：
```js
async function ocrImages(imageBuffers) {
  const url = `${botConfig.ocr_service_url}/ocr/batch`;
  const formData = new FormData();
  imageBuffers.forEach((buf, i) => {
    formData.append("files", new Blob([buf]), `page_${i + 1}.png`);
  });
  const res = await fetch(url, {
    method: "POST",
    body: formData,
    signal: AbortSignal.timeout(120000)  // OCR 多页可能慢，给 2 分钟
  });
  if (!res.ok) throw new Error(`OCR 服务返回 HTTP ${res.status}`);
  const data = await res.json();
  return data.pages;  // [{page, text}, ...]
}
```

**改造 `extractFromPdf`**，处理 >3 页分支：
```js
if (pageCount > 3) {
  // 转图片 buffer 数组（不转 base64，OCR 服务要原始字节）
  const imageBuffers = [];
  for (let i = 1; i <= pageCount; i++) {
    const buf = await renderPdfPageToBuffer(doc, i);  // PNG buffer
    imageBuffers.push(buf);
  }
  const pages = await ocrImages(imageBuffers);
  const fullText = pages.map(p => `## 第 ${p.page} 页\n\n${p.text}`).join("\n\n");
  return { mode: "ocr_text", pageCount, text: fullText };
}
```

**集成到文件分支**：
```js
if (result.mode === "ocr_text") {
  pendingFiles.set(fromId, {
    type: "text",
    fileName: fileItem.file_name,
    text: result.text,
    timestamp: Date.now()
  });
  const charCount = result.text.length;
  await sendMsgSafe(fromId, contextToken,
    `已收到「${fileItem.file_name}」（${result.pageCount}页扫描件），OCR识别出约 ${charCount} 字内容。请告诉我您的要求。`);
}
```

**用户提示优化**：OCR 处理多页耗时，先发"正在识别 N 页 PDF，请稍候..."的中间反馈。

**验收**：传一份 10 页扫描版 PDF，约 15-25 秒内收到"已识别 N 字"提示，后续提问能得到准确答复。

---

### Task 2.4：错误降级与监控

**降级策略**：
- OCR 服务挂了（HTTP 调用失败）→ 提示用户"OCR 服务暂时不可用，请稍后重试"，不阻塞 bot 主流程
- OCR 识别为空（全是空白页或图片质量太差）→ 提示用户"未能识别出文字内容"
- PDF 解析失败 → 现有的 try/catch 已经覆盖

**日志要点**：
- bot.js 端：每次调用 OCR 服务记录页数和耗时
- OCR 服务端：FastAPI 默认 access log 已够用

**健康检查**（可选）：
- bot.js 启动时 ping 一次 `/health`，若失败打印警告但不阻止启动

---

### 阶段二交付物
- [ ] OCR 微服务部署完成，PM2 托管
- [ ] bot.js 集成 OCR 调用
- [ ] >3 页 PDF 端到端跑通
- [ ] OCR 服务异常时的降级提示生效
- [ ] 文档更新（CLAUDE.md 加上 ocr-service 部分）

---

## 验收测试用例

**阶段一完成后**：

| 用例 | 输入 | 期望 |
|------|------|------|
| T1.1 | 发一张包含文字的截图，问"这上面写了什么" | AI 准确读出文字 |
| T1.2 | 发一张产品照片，问"这是什么" | AI 描述产品 |
| T1.3 | 发 2 页 PDF（含图表），问"总结这份文件" | AI 给出总结，体现对图表的理解 |
| T1.4 | 发 5 页 PDF | 收到"暂不支持"友好提示 |
| T1.5 | 发 docx 后追问 | 与旧版行为一致 |
| T1.6 | 不发文件直接对话 | 纯文本对话不受影响 |
| T1.7 | 发 xlsx 后问"第3行是什么" | AI 准确回答 |

**阶段二完成后**：

| 用例 | 输入 | 期望 |
|------|------|------|
| T2.1 | 发 10 页扫描版合同 PDF | 25 秒内返回"已识别 N 字"，后续问答准确 |
| T2.2 | 发 50 页 PDF | 不超时（必要时分批 OCR），最终回复 |
| T2.3 | OCR 服务故意停掉后传扫描 PDF | 收到友好降级提示 |
| T2.4 | 传纯图扫描件（无文字层 PDF） | OCR 识别成功 |

---

## 风险与注意事项

### 风险点

1. **node-canvas 在飞牛 NAS 编译失败**
   - 缓解：直接用 `@napi-rs/canvas` 替代，预编译二进制
   - 或：放弃 canvas，改用 `pdf-to-img`（同样的原生依赖问题）
   - 备选：彻底不用 Node 转图，让 OCR 服务直接接收 PDF 字节，Python 端用 `pdf2image`（依赖 poppler）转图后 OCR

2. **大 PDF 转图片占内存**
   - 50 页 PDF × 2x 缩放可能占 1GB+ 内存
   - 缓解：流式处理，每页 OCR 完立刻释放 buffer；或限制单文件最大页数（如 100 页）

3. **OCR 服务首次冷启动慢**
   - RapidOCR 加载模型约 3-5 秒
   - 缓解：PM2 启动后立即调用一次 `/ocr/image` 预热

4. **中转站 vision 实际能力未验证**
   - 风险：中转站可能"阉割"vision，识别质量打折
   - 缓解：Task 1.1 完成后立即用清晰的中文文字截图测试，质量不达标则向中转站提工单或考虑切到其他渠道

5. **历史记忆塞图片导致 token 爆炸**
   - 已在 Task 1.1 规避：历史只存 text，不存 image base64

### 不在范围内（未来工作）

- 多张图片同时上传（微信本身的批量发图）
- 视频/语音消息处理
- PPT 中的图片（当前 pptx 解析只拿文字）
- PDF 公式/表格结构化还原（如需要可后期接 MinerU 或 GOT-OCR2.0）
- 文件长期记忆（向量化 + RAG 检索，目前每次重新读全文）

---

## 开发顺序建议

```
Day 1（2-3h）：
  Task 1.1 → Task 1.2 → Task 1.3
  里程碑：图片消息端到端跑通

Day 2（2-3h）：
  Task 1.4 → Task 1.5（可选）
  里程碑：≤3页 PDF 跑通，阶段一收尾

Day 3（2-3h）：
  Task 2.1 → Task 2.2
  里程碑：OCR 服务独立可用

Day 4（2-3h）：
  Task 2.3 → Task 2.4
  里程碑：>3页 PDF 端到端跑通，全部验收
```

每个 Task 完成后建议 git commit 一次，便于回滚。

---

## 给开发者的工作约定

1. **不修改现有纯文本对话路径**，所有改动通过新增分支或扩展函数签名实现，确保旧功能零回归
2. **每个 Task 单独 commit**，commit message 用约定式：`feat(image): support image content block in callAI`
3. **不删除现有 `mammoth`/`.docx` 处理代码**
4. **配置项变更需同步更新 `CLAUDE.md`**
5. **遇到飞牛 NAS 特有的依赖编译问题**，先尝试 `@napi-rs/canvas` 这类预编译包，避免装系统 dev 包
6. **OCR 服务的 Python 代码与 bot.js 完全解耦**，通过 HTTP 通信，不共享任何状态
7. **测试用的 PDF 和图片**放在 `instances/<name>/test-files/`，gitignore
