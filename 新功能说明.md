# bot.js 新增功能说明

## 概述

bot.js 新增了**图片理解**和 **PDF 处理**能力，同时扩展了对 **xlsx/pptx** 文件的支持。所有改动向后兼容，现有纯文本对话路径不受影响。

---

## 一、图片消息支持

### 用法

直接向 Bot 发送图片，Bot 会提示已接收，随后回复文字描述你的需求（如"描述这张图"、"图上写了什么"），Bot 会将图片和文字一起发送给 AI 进行视觉理解。

### 支持格式

JPEG、PNG、GIF、WebP

### 实现原理

- 图片下载后通过文件头 magic number 自动检测格式
- 转为 base64 后以 `image` content block 传给 Claude vision
- 图片 base64 **不会**存入对话历史，避免 token 膨胀

---

## 二、PDF 文件支持

### 用法

向 Bot 发送 PDF 文件，Bot 会根据页数自动选择处理策略：

| 页数 | 处理方式 | 耗时 |
|------|---------|------|
| ≤ 3 页 | 每页渲染为图片，走 Claude vision | 数秒 |
| > 3 页 | 每页渲染为图片，调用本地 OCR 提取文字 | 15-25 秒（10 页） |

### ≤ 3 页 PDF（vision 通道）

- 保留版式、图表、表格等视觉信息
- AI 能基于图片内容回答，适合图文混排的短文档

### > 3 页 PDF（OCR 通道）

- 需要配置 OCR 服务（见下方"配置说明"）
- 每页渲染为 2x 缩放 PNG，调用本地 RapidOCR 批量识别
- 识别结果以文字形式传给 AI，省 token

### 空页处理

如果 OCR 返回为空（如纯图片扫描件质量低），Bot 会提示"未能识别出文字内容"。

---

## 三、xlsx/pptx 文件支持

| 格式 | 处理方式 | 说明 |
|------|---------|------|
| `.xlsx` / `.xls` | SheetJS 转 CSV | 每个 sheet 独立提取，带标题 |
| `.pptx` | node-pptx-parser 提取文字 | 每页 slide 的文字内容，图片会丢失 |

### 用法

直接发送文件，Bot 提取文本内容后暂存（30 分钟有效），用户下一条消息时文本与要求合并一起传给 AI。

---

## 四、OCR 微服务

### 架构

```
┌─────────────┐     HTTP      ┌──────────────────────┐
│   bot.js    │ ─────────────>│ ocr-service (Python) │
│  (Node.js)  │               │  FastAPI + RapidOCR  │
└─────────────┘               └──────────────────────┘
```

### 配置

在实例 `config.json` 中添加：

```json
{
  "ocr_service_url": "http://127.0.0.1:8765"
}
```

OCR 服务通过 PM2 管理，已配置自动重启。bot 启动时会对 OCR 服务做健康检查，若不可用会打印警告但不阻止 bot 正常运行。

### 端点

| 端点 | 方法 | 说明 |
|------|------|------|
| `/ocr/image` | POST | 单张图片 OCR，返回文本 |
| `/ocr/batch` | POST | 批量图片 OCR，返回每页文本 |
| `/health` | GET | 健康检查 |

### 降级策略

- OCR 服务不可用 → 提示"OCR 服务暂时不可用"，bot 主流程不受影响
- OCR 识别为空 → 提示"未能识别出文字内容"
- 未配置 `ocr_service_url` → >3 页 PDF 提示"暂未配置 OCR 服务"

---

## 五、支持的文件格式汇总

| 格式 | 处理方式 | vision / text |
|------|---------|---------------|
| `.txt` | 读 UTF-8 文本 | text |
| `.docx` | mammoth 提取文字 | text |
| `.xlsx` `.xls` | SheetJS → CSV | text |
| `.pptx` | 提取每页文字 | text |
| `.pdf` ≤3 页 | 渲染为 PNG → base64 | vision |
| `.pdf` >3 页 | 渲染为 PNG → OCR → 文字 | text |
| 图片 (type=2) | 解密 → 检测格式 → base64 | vision |

---

## 六、重启服务

```bash
# 启动 OCR 服务
pm2 start ecosystem.config.cjs --only ocr-service

# 重启 bot 实例（任选）
pm2 restart weixin-bot-1
pm2 restart weixin-bot-2
pm2 restart weixin-bot-test

# 保存 PM2 配置（NAS 重启后自动恢复）
pm2 save
```
